diff --git a/CMakeLists.txt b/CMakeLists.txt
index 14f23e8..dab398a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,4 +1,4 @@
-cmake_minimum_required(VERSION 3.1)
+cmake_minimum_required(VERSION 3.20)
 project(SoundTouch VERSION 2.3.2 LANGUAGES CXX)
 
 include(GNUInstallDirs)
@@ -7,7 +7,7 @@ if(MSVC)
   set(COMPILE_DEFINITIONS /O2 /fp:fast)
   set(COMPILE_OPTIONS )
 else()
-  set(COMPILE_OPTIONS -Ofast)
+  set(COMPILE_OPTIONS -O3 -ffast-math)
 endif()
 
 #####################
@@ -59,7 +59,9 @@ else()
   target_compile_definitions(SoundTouch PRIVATE SOUNDTOUCH_FLOAT_SAMPLES)
 endif()
 
-if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(armv7.*|armv8.*|aarch64.*)$")
+# The -mfpu=neon flag is only necessary on 32-bit arm.  The neon
+# hardware is mandatory on 64-bit arm.
+if(CMAKE_SYSTEM_PROCESSOR MATCHES "^(armv7.*)$")
   set(NEON_CPU ON)
 else()
   set(NEON_CPU OFF)
