# The following will run the mythfilldatabase shell script at 18:00 every day.  Format is
# minutes hour day_of_month month day_of_week
# In the case of mythfilldatabase, we want it to run at 1800 daily.
# man 5 crontab for more information
# Need to use a shell script because the QTDIR environment variable isn't
# being honored by crontab?
0 18 * * * /bin/bash $HOME/mythtv/MC/programs/mythfilldatabase/mythfilldatabasecron
